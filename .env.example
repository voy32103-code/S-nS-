# Local-only Docker template. Never reuse these values in production.
POSTGRES_PASSWORD=local-only-change-me
REDIS_PASSWORD=local-only-change-me
SANSO_FIELD_ENCRYPTION_KEY_BASE64=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
SANSO_FIELD_ENCRYPTION_KEY_VERSION=local-v1
VITE_API_URL=http://localhost:5080

# Production identity/provider variables remain intentionally absent until approved.

