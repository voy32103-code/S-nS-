<Project Sdk="Microsoft.NET.Sdk.Web">
  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>
  <ItemGroup>
    <!-- Retained for history; the hardened canonical implementation is authoritative. -->
    <Compile Remove="Security/TenantFieldProtector.cs" />
  </ItemGroup>
</Project>
