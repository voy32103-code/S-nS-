import AxeBuilder from'@axe-core/playwright';import{expect,test}from'@playwright/test';
const email='e2e.viewer@example.invalid',password='Long-Safe-E2E-Viewer-Password-2026!',tenant='tenant-an-nhien';
test('viewer sees an accessible denied onboarding state',async({page})=>{await page.goto('/index-v8.html');const inputs=page.locator('form input');await inputs.nth(0).fill(email);await inputs.nth(1).fill(password);await inputs.nth(2).fill(tenant);await page.locator('form button').click();await expect(page.locator('aside')).toBeVisible();await page.locator('nav button').nth(2).click();await expect(page.locator('.denied')).toBeVisible();await expect(page.locator('.denied')).toContainText(/không có quyền/i);const result=await new AxeBuilder({page}).withTags(['wcag2a','wcag2aa','wcag21a','wcag21aa']).analyze();expect(result.violations).toEqual([])});
