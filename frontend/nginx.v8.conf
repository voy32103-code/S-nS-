server {
  listen 8080;
  server_name _;
  root /usr/share/nginx/html;
  index index-v8.html;
  add_header X-Content-Type-Options nosniff always;
  add_header X-Frame-Options DENY always;
  add_header Referrer-Policy no-referrer always;
  location / { try_files $uri $uri/ /index-v8.html; }
}

