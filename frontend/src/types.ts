export type Dashboard={gross:number;expectedPayout:number;actualPayout:number;difference:number;matched:number;needsReview:number;dataFreshness:string;taxStatus:string};export type Line={orderCode:string;type:string;amount:number;explanation:string};export type Reconciliation={settlementCode:string;gross:number;fees:number;refunds:number;expected:number;actual:number;difference:number;status:string;lines:Line[]};
